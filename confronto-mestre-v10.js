(async function(){
 const PERICIAS=[["Furtividade","AGI"],["Iniciativa","AGI"],["Investigação","APT"],["Medicina","APT"],["Percepção","PRE"],["Pontaria","AGI"],["Esquiva","AGI"],["Bloqueio","FOR"],["HtH","FOR"],["Esgrima","AGI"],["Combate com haste","FOR"],["Sobrevivência","APT"],["Vontade","PRE"]];
 function esperarAuth(){if(window.RPG_AUTH)return Promise.resolve(window.RPG_AUTH);return new Promise(r=>document.addEventListener("rpg:auth-pronto",e=>r(e.detail),{once:true}))}
 const auth=await esperarAuth(),qs=new URLSearchParams(location.search),confrontoId=qs.get("confronto"),rpg=document.body.dataset.rpg;if(auth?.perfil!=="mestre"||!confrontoId)return;
 const root=document.querySelector("#confronto-root");
 async function foto(path){if(!path)return "";const {data}=await auth.cliente.storage.from("personagens").createSignedUrl(path,3600);return data?.signedUrl||""}
 function barra(nome,atual,max,classe){const pct=max?Math.min(100,Math.max(0,atual/max*100)):0;return `<div class="recurso ${classe}"><div class="recurso-titulo"><span>${nome}</span><strong>${atual} / ${max}</strong></div><div class="recurso-trilho"><i style="--valor:${pct}%"></i></div></div>`}
 function atributos(p,mini=false){return `<div class="${mini?"mc-atributos":"atributos atributos-confronto"}">${mini?"":'<svg class="atributos-sigilo" viewBox="0 0 390 310" aria-hidden="true"><circle cx="195" cy="157" r="95"/><circle cx="195" cy="157" r="69"/><path d="M195 22 275 270 66 117 324 117 115 270Z"/><path d="M195 60 258 250 99 132 291 132 132 250Z"/></svg>'}${[["agilidade","Agilidade","agilidade"],["forca","Força","forca"],["apt_magica","Apt. Mágica","aptidao"],["presenca","Presença","presenca"],["resistencia","Resistência","resistencia"]].map(([c,n,classe])=>mini?`<span>${n}<b>${p[c]??0}</b></span>`:`<div class="atributo ${classe}"><strong>${p[c]??0}</strong><small>${n}</small></div>`).join("")}${mini?"":'<span class="titulo-pentagrama">Atributos</span>'}</div>`}
 function miniFicha(p){return `<section class="mini-combatente"><header><small>Combatente vinculado</small><h2></h2></header>${atributos(p,true)}<p class="mini-defesa">Defesa <strong>${p.defesa??10}</strong></p>${barra("Vida",p.vida_atual??0,p.vida_max??0,"vida")}${barra("Energia mágica",p.energia_atual??0,p.energia_max??0,"energia")}${barra("Sanidade",p.sanidade_atual??0,p.sanidade_max??0,"sanidade")}</section>`}
 function rolar(faces){const valor=Math.floor(Math.random()*faces)+1,out=document.querySelector("#resultado-dados");out.textContent=`D${faces}: ${valor}`;out.classList.toggle("resultado-critico",valor===faces)}
 async function carregar(){
  const {data,error}=await auth.cliente.rpc("obter_confronto_detalhes",{confronto_alvo:confrontoId});if(error||!data){root.innerHTML=`<p>${error?.message||"Confronto não encontrado."}</p>`;root.hidden=false;return}
  let ficha={...data.lado_b},descricao="",drops="",xp_recompensa=0,ficha_livre="";
  if(data.confronto.oponente_tipo==="npc"){const {data:npc}=await auth.cliente.from("npcs_monstros").select("*").eq("id",ficha.id).single();if(npc)ficha={...ficha,...npc};descricao=ficha.descricao||"";drops=ficha.drops||"";xp_recompensa=ficha.xp_recompensa??0;ficha_livre=ficha.ficha_livre||""}
  const jogador=data.lado_a,img=await foto(ficha.foto_path),campanha=data.confronto.campanha;
  root.innerHTML=`<nav class="ficha-topo confronto-topo"><a class="ficha-logo" href="../personagens/index.html?campanha=${encodeURIComponent(campanha)}">MESTRE · ${rpg==="abismo"?"ABISMO":"MARVEL RPG"}</a><div class="ficha-abas"><button class="ficha-aba ativa" data-aba="combate">Combate</button><button class="ficha-aba" data-aba="drops">Drops</button><button class="ficha-aba" data-aba="xp">XP</button><button class="ficha-aba" data-aba="descricao">Descrição</button></div><div class="confronto-acoes"><button id="encerrar-confronto" type="button">Encerrar confronto</button><a class="voltar compacto" href="../personagens/index.html?campanha=${encodeURIComponent(campanha)}">← Escudo</a></div></nav><p id="confronto-status">${data.confronto.ativo?"Confronto ativo em tempo real":"Confronto encerrado"}</p>
  <section class="painel-confronto" data-painel="combate"><section class="ficha-grade"><section class="ficha-coluna ficha-identidade"><div class="identidade-cabecalho"><div class="retrato-wrap">${img?`<img class="ficha-retrato" src="${img}" alt="">`:'<div class="retrato-vazio">?</div>'}</div><dl class="identidade-dados"><div><dt>Personagem</dt><dd id="nome-oponente"></dd></div><div><dt>Origem</dt><dd>${ficha.tipo||"Personagem"}</dd></div><div><dt>Controlado por</dt><dd>Mestre</dd></div><div><dt>Classe</dt><dd>Desconhecida</dd></div></dl></div>${atributos(ficha)}<div class="progresso-personagem"><span>Nível <strong>${ficha.nivel??0}</strong></span><span>Defesa <strong>${ficha.defesa??10}</strong></span></div>${barra("Vida",ficha.vida_atual??0,ficha.vida_max??0,"vida")}${barra("Energia mágica",ficha.energia_atual??0,ficha.energia_max??0,"energia")}${barra("Sanidade",ficha.sanidade_atual??0,ficha.sanidade_max??0,"sanidade")}</section><section class="ficha-coluna ficha-pericias"><h1>Perícias</h1><div class="tabela-pericias"><div class="pericia cabeca"><span>Perícia</span><span>Atributo</span><span>Bônus</span><span>Treino</span></div><div id="pericias-oponente">${PERICIAS.map(([n,a])=>`<button class="pericia" data-pericia="${n}" type="button"><span>${n}</span><span>(${a})</span><span>0</span><span>0</span></button>`).join("")}</div></div></section><section class="ficha-coluna ficha-combate"><h1>Combate</h1><section class="rolar-dados dados-novos"><h2>Rolar dados</h2><p>Escolha o dado usado pelo NPC ou monstro.</p><div class="seletor-dados">${[4,6,8,10,20].map(n=>`<button type="button" data-dado="${n}">D${n}</button>`).join("")}</div><output id="resultado-dados" class="resultado-dados">Aguardando rolagem</output></section>${miniFicha(jogador)}<div class="respiro-confronto"></div></section></section></section>
  <section class="painel-confronto painel-texto" data-painel="drops" hidden><span>Recompensas do confronto</span><h1>Drops</h1><div class="lista-drops"></div></section><section class="painel-confronto painel-texto" data-painel="xp" hidden><span>Recompensa de experiência</span><h1>${xp_recompensa} XP</h1><p>Este valor será concedido quando a regra de vitória do confronto for implementada.</p></section><section class="painel-confronto painel-texto" data-painel="descricao" hidden><span>Arquivo do oponente</span><h1>Descrição</h1><p id="descricao-oponente"></p><h2>Ficha livre</h2><p id="ficha-livre-oponente"></p></section>`;
  root.querySelector("#nome-oponente").textContent=ficha.nome;root.querySelector("#descricao-oponente").textContent=descricao||"Nenhuma descrição registrada.";root.querySelector("#ficha-livre-oponente").textContent=ficha_livre||"Nenhuma anotação registrada.";const lista=root.querySelector(".lista-drops"),itens=drops.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);lista.replaceChildren(...(itens.length?itens:["Nenhum drop cadastrado."]).map(x=>{const p=document.createElement("p");p.textContent=x;return p}));
  root.querySelectorAll("[data-dado]").forEach(b=>b.onclick=()=>rolar(Number(b.dataset.dado)));root.querySelectorAll("[data-pericia]").forEach(b=>b.onclick=()=>rolar(20));root.querySelectorAll(".ficha-aba").forEach(b=>b.onclick=()=>{root.querySelectorAll(".ficha-aba").forEach(x=>x.classList.toggle("ativa",x===b));root.querySelectorAll("[data-painel]").forEach(x=>x.hidden=x.dataset.painel!==b.dataset.aba)});
  const encerrar=root.querySelector("#encerrar-confronto");encerrar.disabled=!data.confronto.ativo;encerrar.onclick=async()=>{encerrar.disabled=true;const {error}=await auth.cliente.rpc("encerrar_confronto_mestre",{confronto_alvo:confrontoId});root.querySelector("#confronto-status").textContent=error?error.message:"Confronto encerrado"};root.hidden=false;
 }
 await carregar();
})();
